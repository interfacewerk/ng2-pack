import { ColumnConfig, SortingMode } from './../types';
import { ColumnState } from './../column-state.class';
import { SortColumnEvent, ToggleSubfieldEvent } from '../events';
import { TableComponent } from './../table.component';
import { EventEmitter, OnInit } from '@angular/core';
export declare class ThComponent implements OnInit {
    column: ColumnState;
    changeVisibility: boolean;
    rowsSortingMode: SortingMode;
    visibleColumns: string[];
    columnsConfig: ColumnConfig[];
    removeColumn: EventEmitter<string>;
    sortColumn: EventEmitter<SortColumnEvent>;
    addCombobox: EventEmitter<number>;
    toggleSubfield: EventEmitter<ToggleSubfieldEvent>;
    private _rowsSortingMode;
    private _visibleColumns;
    private _columnsConfig;
    private tableComponent;
    constructor(tableComponent: TableComponent);
    ngOnInit(): void;
    readonly hasAllColumnsVisble: boolean;
    readonly isLastColumn: boolean;
    isSortingDisabled(column: ColumnState): boolean;
    showSortIcon(column: ColumnState, sortType: string, direction: string): boolean;
    onSortColumn(column: ColumnState, direction?: string): void;
    onRemoveColumn(columnName: string): void;
    onToggleSubfield(column: ColumnState, subfieldName: string): void;
    private delegateInput;
}
