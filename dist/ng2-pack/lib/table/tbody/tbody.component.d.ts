import { TdComponent } from './../td/td.component';
import { TableInitService } from './../table-init.service';
import { ColumnConfig, ColumnLookup } from '../types';
import { ColumnState } from './../column-state.class';
import { EditCellEvent, RowClickEvent } from '../events';
import { TableComponent } from './../table.component';
import { AfterViewInit, ElementRef, EventEmitter } from '@angular/core';
export declare class TbodyComponent implements AfterViewInit {
    private elementRef;
    private tableInitService;
    addingColumnIndex: number;
    inlineEditingEnabled: boolean;
    rows: any[];
    columnsConfig: ColumnConfig[];
    visibleColumns: string[];
    rowClick: EventEmitter<RowClickEvent>;
    editCell: EventEmitter<EditCellEvent>;
    customTemplate: boolean;
    private _columnsConfig;
    private _columnsLookup;
    private _rows;
    private _visibleColumns;
    private isEditable;
    private tableComponent;
    private customCells;
    private cellTemplates;
    constructor(elementRef: ElementRef, tableInitService: TableInitService, tableComponent: TableComponent);
    ngAfterViewInit(): void;
    readonly columnsLookup: ColumnLookup;
    readonly changeColumnVisibility: boolean;
    isSorted(column: ColumnState, direction: string): boolean;
    column(columnName: string): ColumnState;
    onRowClicked(rowIndex: number, rowObject: any): void;
    onEditCell(tdComponent: TdComponent, rowIndex: number): void;
    private delegateInput;
}
